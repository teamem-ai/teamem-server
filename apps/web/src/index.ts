/**
 * teamem portal web UI (AGPL-3.0-only)
 *
 * UI implementation is scheduled for M2 (shadcn/ui + React + Vite SPA,
 * served directly by the server). W1-2 covers information architecture
 * and wireframes only — no UI code before that lands.
 */
export {};
