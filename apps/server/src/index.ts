/**
 * teamem portal server (AGPL-3.0-only)
 *
 * M0 scope: ingestion API (/v1/events), pg-boss compile queue, GitHub
 * connector, F1 typed extraction. Implementation starts after contract v0
 * freeze and M0 task breakdown.
 *
 * Project red line: nothing ships that only *looks* functional — no
 * placeholder endpoints, no mock data in production code paths.
 */
export {};
